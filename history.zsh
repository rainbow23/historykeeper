#!/bin/sh
# historys=$(history -i 0)
# historys=$(exec history -E)
# historys=$(exec $SHELL -l | history.zsh)
# historys=$(exec $SHELL -l -c history -E)

# history
date
# echo $historys


# exec -l $SHELL -c "history.zsh"

# exec -l "${SHELL}" -c 'exec "history.zsh"'
# exec -l $SHELL -c "exec history.zsh"
# exec -l $SHELL -c "history.zsh"

# exec -l $SHELL -c "source history.zsh"


# /Users/rainbow/go/src/historyKeeper/history.zsh


echo $HISTFILE
